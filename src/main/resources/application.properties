# --- Nombre de la aplicación y puerto ---
spring.application.name=sistemaweb
server.port=8080

# --- Configuración de la base de datos Supabase ---
spring.datasource.url=jdbc:postgresql://aws-0-us-east-2.pooler.supabase.com:5432/postgres?sslmode=require
spring.datasource.username=postgres.jhljgrjlvvkiikahjocv
spring.datasource.password=DataBase320EGC
spring.datasource.driver-class-name=org.postgresql.Driver

# --- HikariCP (pool de conexiones) ---
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=300000   
# máximo 5 minutos para evitar cierre inesperado
spring.datasource.hikari.connection-timeout=30000

# --- JPA / Hibernate ---
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate   # valida tablas, evita errores de ALTER
# Opcional:
# spring.jpa.hibernate.ddl-auto=update   # solo si quieres que Hibernate actualice columnas automáticamente

# --- Thymeleaf ---
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML5
spring.thymeleaf.cache=false

# --- Logging reducido ---
logging.level.root=WARN
logging.level.org.springframework=WARN
logging.level.org.hibernate=WARN
logging.level.org.thymeleaf=WARN

# --- Otros ---
server.error.whitelabel.enabled=false
